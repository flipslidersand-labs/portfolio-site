name: Quality

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    uses: flipslidersand-labs/qa-workflows/.github/workflows/node-test.yml@main
    with:
      node-version: "20"
      package-manager: "npm"
      run-lint: false
      run-test: false
